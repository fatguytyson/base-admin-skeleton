{% extends 'base.html.twig' %}

{% block title %}{{ title }}{{ parent() }}{% endblock %}

{% block content %}
    {{ content | raw }}
    {% for flag in flags if flag != '' %}
        {% include flag %}
    {% endfor %}
{% endblock %}