{% extends 'base.html.twig' %}

{% block title %}Rating edit{{ parent() }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-xs-12">
            <h1 class="page-header">Rating edit</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-xs-12">
            <div class="panel panel-default">
                {{ form_start(edit_form) }}
                <div class="panel-heading">
                    <input type="submit" value="Edit" class="btn btn-success pull-right" />
                    Create Rating
                </div>
                <div class="panel-body">
                    {{ form_widget(edit_form) }}
                </div>
                {{ form_end(edit_form) }}
                <div class="panel-footer">
                                        <a href="{{ path('admin_rating_index') }}" class="btn btn-default">Back to the list</a><div class="pull-right">
    {{ form_start(delete_form) }}
    <input type="submit" value="Delete" class="btn btn-danger">
    {{ form_end(delete_form) }}
</div></div>
            </div>
        </div>
    </div>

{% endblock %}
